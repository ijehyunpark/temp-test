language: java
jdk:
  - openjdk11
  
branches:
  only:
  - master
    
cache:
  directories:
  - "$HOME/.m2/repository"
  - '$HOME/.gradle'
  
script: "./gradlew clean build"

services:
  - mysql

before_install:
  - chmod +x gradlew
  - mysql -e 'CREATE DATABASE cbot'